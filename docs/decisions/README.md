# Decisions

One file per slice: the questions each worker hit and the assumption it continued with, plus `contract-errata.md`, where the merged code differs from `docs/CONTRACT.md`. New slices write `docs/decisions/<slice>.md`, never a file at the repo root.
